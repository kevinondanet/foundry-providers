feat(notify): retry transient delivery failures with linear backoff

- add attempts and backoff_seconds keyword-only options to send
- log failed attempts through the module logger
- add a retry test with a mocked transport

🔁 - Generated by Copilot
